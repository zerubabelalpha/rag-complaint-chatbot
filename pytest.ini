[pytest]
markers =
    core: Mark tests as part of the core functionality suite.
    slow: Mark tests that are slow (e.g., loading real models).
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
